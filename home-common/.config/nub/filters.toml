# User-global NUB filters — apply to all your projects.
# Project-local .nub/filters.toml takes precedence over these.
# Docs: https://github.com/sustinbebustin/nub#custom-filters
schema_version = 1

# Example: suppress noise from a tool you use everywhere
# [filters.my-global-tool]
# description = "Compact my-global-tool output"
# match_command = "^my-global-tool\\b"
# strip_ansi = true
# strip_lines_matching = ["^\\s*$"]
# max_lines = 40
